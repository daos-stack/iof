
"""Build ping test"""
Import('ENV PREREQS')

TENV=ENV.Clone()
TENV.Append(CPPPATH=['common/include'])
COMMON = TENV.Object('common/version')

TENV.Program(['ionss/ionss.c', COMMON])
TENV.Program(['cnss/cnss.c', COMMON])
